<nav class="navbar navbar-expand-lg mb-3">
  <div class="container">
    <a class="navbar-brand" onclick="openNav()" href="#"><i class="bi bi-list"></i></a>
    <div class="row">
      <div class="col">
        <% if user_signed_in? %>
          <% if current_user.image.attached? %>
            <span><%= link_to  image_tag(current_user.image, class:"avatar-navbar rounded-circle") , edit_user_registration_path, :class=>"btn"%></span>
          <% else %>
            <span> <%= link_to  current_user.name , edit_user_registration_path, :class=>"btn btn-outline-secondary"%></span>
          <% end %>
        <% else %>
          <span> <%= link_to "Login" , new_user_session_path, :class=>"btn btn-outline-secondary"%></span>
        <% end %>
      </div>
    </div>
  </div>
</nav>